name: Validate Datasets YAML

on:
  push:
    branches: [main]
    paths:
      - "README.md"
      - "metadata/overrides.yaml"
      - "scripts/parse_readme.py"
  pull_request:
    branches: [main]
    paths:
      - "README.md"
      - "metadata/overrides.yaml"
      - "scripts/parse_readme.py"

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pyyaml pydantic

      - name: Run parser
        run: python scripts/parse_readme.py

      - name: Check for drift in datasets.yaml
        run: |
          if ! git diff --exit-code metadata/datasets.yaml; then
            echo "::error::datasets.yaml is out of sync with README.md. Run parse_readme.py locally and commit the result."
            exit 1
          fi

      - name: Run merge_datasets.py
        run: python scripts/merge_datasets.py

      - name: Commit and push datasets_full.yaml if changed
        run: |
          if ! git diff --exit-code metadata/datasets_full.yaml; then
            git config user.name "github-actions"
            git config user.email "github-actions@github.com"
            git add metadata/datasets_full.yaml
            git commit -m "ci: update datasets_full.yaml [auto]"
            git push
          fi

      - name: Validate datasets.yaml with Pydantic
        run: python scripts/validate_datasets.py